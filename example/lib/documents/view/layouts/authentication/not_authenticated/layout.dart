import 'package:flutter/widgets.dart';

class NotAuthenticatedLayout extends StatelessWidget {
  const NotAuthenticatedLayout({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
