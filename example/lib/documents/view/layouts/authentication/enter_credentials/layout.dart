import 'package:flutter/widgets.dart';

class EnterCredentialsLayout extends StatelessWidget {
  const EnterCredentialsLayout({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
