import 'package:flutter/widgets.dart';

class AuthenticatingLayout extends StatelessWidget {
  const AuthenticatingLayout({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
